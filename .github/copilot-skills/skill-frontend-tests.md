# Skill: Write Frontend Tests

1. For components, create `.component.spec.ts` in the same component folder
2. Use Jest + Angular Testing Library
3. Mock HttpClient using HttpTestingController
4. Test user interactions
5. Avoid testing implementation details
6. Follow the component folder structure with separate `.component.ts/.html/.css` files
